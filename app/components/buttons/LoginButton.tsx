const LoginButton = () => {
  return (
    <div>
      <button
        className="text-[20px] text-[#ffffff] cursor-pointer font-semibold"
        type="button"
      >
        Login
      </button>
    </div>
  );
};

export default LoginButton;
